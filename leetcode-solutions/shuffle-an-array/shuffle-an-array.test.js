const { Solution } = require("./shuffle-an-array");

describe("Solution", () => {
  test("Example test case 1", () => {
    const solution = new Solution([1, 2, 3])
    // TODO not sure how to test this one yet?
  });
});